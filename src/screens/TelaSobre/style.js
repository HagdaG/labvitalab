import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        padding: 30,
        alignItems: 'center',
        backgroundColor: '#fff',
        flex: 1
    },
    info: {
        marginBottom: 20,
        fontSize: 18,
        color: '#000'
    },
    info2: {
        fontSize: 18,
        textAlign: 'center',
        color: '#000'
    },
    info3: {
        marginBottom: 20,
        fontSize: 18,
        textAlign: 'justify',
        color: '#000'
    }
})

export default styles