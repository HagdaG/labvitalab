import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        padding: 30,
        alignItems: 'center',
        backgroundColor: '#fff',
        flex: 1
    },
    title: {
        fontWeight: "bold",
        marginBottom: 20,
        fontSize: 18,
        color: '#000'
    },
    info: {
        marginBottom: 20,
        fontSize: 18,
        color: '#000'
    },
    button: {
        padding: 10,
        borderRadius: 10,
        marginBottom: 20,
        width: '100%',
        backgroundColor: '#000'
    },
    textButton: {
        fontSize: 18,
        textAlign: 'center',
        color: '#fff'
    }
})

export default styles